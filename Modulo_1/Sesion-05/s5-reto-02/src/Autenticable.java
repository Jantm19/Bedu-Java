/**
 * Interfaz que define el contrato para la autenticación de un método de pago.
 * Contiene un método autenticar() que devuelve un valor booleano.
 */
public interface Autenticable {
    boolean autenticar();
}
